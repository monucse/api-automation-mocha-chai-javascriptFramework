version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:14

    steps:
      - checkout

      - run:
          name: Install Dependencies
          command: npm install

      - run:
          name: Build and Test
          command: npm test

      - run:
          name: Deploy
          command: echo "Deployed successfully"

workflows:
  version: 2
  build:
    jobs:
      - build
